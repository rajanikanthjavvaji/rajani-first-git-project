apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-crash
spec:
  replicas: 3
  selector:
    matchLabels:
      app: nginx-crash
  template:
    metadata:
      labels:
        app: nginx-crash
    spec:
      containers:
        - name: nginx
          image: nginx:latest
          